apiVersion: admissionregistration.k8s.io/v1
kind: MutatingAdmissionPolicyBinding
metadata:
  name: formal-kubernetes-egress
spec:
  policyName: formal-kubernetes-egress
  matchResources:
    objectSelector:
      matchLabels:
        formal.ai/kubernetes-egress-inject: "true"
